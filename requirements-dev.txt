# Everything in the runtime image, plus what is needed to build a corpus,
# benchmark the pipeline and run the tests. Not installed in the container.
-r requirements.txt

# --- data ingestion: streams MSMARCO-XI parquet shards over HTTP range reads ---
pyarrow==22.0.0
fsspec==2025.9.0
aiohttp==3.13.2
requests==2.32.5

# --- verification ---
pytest==8.4.2
pytest-asyncio==1.1.0
ruff==0.12.5
